# 声作隐私说明

更新日期：2026-08-21

## 一句话说明

声作在本机完成声音克隆和配音，不提供账号、云端语音、遥测或广告服务。模型安装需要联网下载固定版本的运行文件和权重，但应用不会把用户的录音、文稿或生成结果发送给模型下载站点。用户也可以选择配置自己的文本 API；只有主动使用“智能处理”或“智能提取角色”时，对应的文字才会发送到该接口。

## 本机保存的数据

声作会在当前 Windows 用户可见或专用的数据目录中保存：

- Windows“文档\声作声音库”：用户主动导入的授权录音、录音原文和声音档案。
- `%LOCALAPPDATA%\声作模型库` 或用户自行选择的位置：按需下载的模型、独立运行文件、缓存、安装收据和校验记录。
- 声作应用数据目录：项目、任务队列、生成的 MP3 和必要的结果元数据。

这些内容不会随完整便携包一起分发。用户可在应用中打开模型文件夹；删除声音请使用“我的声音”中的删除操作，删除模型前应先退出声作。

## 网络访问

首次安装模型或补全运行文件时，声作只访问模型插件清单允许的 HTTPS 来源，用于下载 Python、依赖、官方源码和权重。用户主动切换下载源时，文件仍须通过固定版本和校验规则。模型准备完成后，正常本地生成不依赖云端语音服务。

文字智能功能只有完成 API 配置后才能使用。使用“智能处理”时会发送选中的文稿、处理要求和当前模型能力；使用“智能提取角色”时会发送粘贴框中的全文和本地角色提取规则。两种操作都不会发送录音、声音档案、生成音频、项目文件或本地路径。API Key 由 Windows 安全存储加密，界面只能看到是否已保存，不能读取明文。接口提供方如何保存和使用文本取决于用户选择的服务，使用前应查看该服务的隐私规则。

设置页的“投喂”只打开随软件内置的静态收款码图片。声作不接入支付 SDK，不发起支付请求，也不读取、保存或统计交易信息。

本地 Worker 只监听 `127.0.0.1` 随机端口，不向局域网或公网提供服务。启动握手使用一次性令牌，后续请求使用仅保存在内存中的会话令牌。

## 不收集的内容

当前版本不包含：

- 账号、手机号、邮箱或身份档案；
- 会员、支付账户、交易信息或广告；
- 使用统计、崩溃遥测或行为分析；
- 云端保存、云端训练或第三方云语音生成。

## 用户责任

只应导入本人声音，或已经获得声音所有者明确授权的录音。用户应对声音授权、生成内容、发布渠道和商业用途负责。删除本机文件前请自行备份需要保留的录音和成品。

## 诊断信息

技术日志只应包含版本、模型 ID、状态、耗时、脱敏硬件摘要和内部错误码，不应记录完整文稿、原始录音、访问令牌或不必要的私人路径。若需要向维护人员反馈问题，请先检查文件中是否包含私人信息。
