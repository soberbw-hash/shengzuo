# 声作隐私说明

更新日期：2026-08-15

## 一句话说明

声作在本机完成声音克隆和配音，不提供账号、云端语音、遥测或广告服务。模型安装需要联网下载官方文件，但应用不会把用户的录音、文稿或生成结果发送给模型下载站点。

## 本机保存的数据

声作会在当前 Windows 用户的数据目录中保存：

- `voices`：用户主动导入的授权录音、录音原文和声音档案。
- `engines`：按需下载的模型、隔离运行环境、缓存、安装收据和校验记录。
- 各模型的 `outputs`：生成的 MP3 和必要的结果元数据。

这些内容不会随完整便携包一起分发。用户可在应用中打开模型文件夹；删除声音请使用“我的声音”中的删除操作，删除模型前应先退出声作。

## 网络访问

首次安装模型或补全运行环境时，声作只访问模型插件清单允许的官方 HTTPS 来源，用于下载 Python、依赖、官方源码和权重。模型准备完成后，正常本地生成不依赖云端语音服务。

本地 Worker 只监听 `127.0.0.1` 随机端口，不向局域网或公网提供服务。启动握手使用一次性令牌，后续请求使用仅保存在内存中的会话令牌。

## 不收集的内容

当前版本不包含：

- 账号、手机号、邮箱或身份档案；
- 会员、支付或广告；
- 使用统计、崩溃遥测或行为分析；
- 云端保存、云端训练或第三方云语音生成。

## 用户责任

只应导入本人声音，或已经获得声音所有者明确授权的录音。用户应对声音授权、生成内容、发布渠道和商业用途负责。删除本机文件前请自行备份需要保留的录音和成品。

## 诊断信息

技术日志只应包含版本、模型 ID、状态、耗时、脱敏硬件摘要和内部错误码，不应记录完整文稿、原始录音、访问令牌或不必要的私人路径。若需要向维护人员反馈问题，请先检查文件中是否包含私人信息。
