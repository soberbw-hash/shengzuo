# 更新记录

## 1.0.0 · 2026-08-21

声作的首个正式版本。

### 声音与配音

- 支持 VoxCPM2、Fun-CosyVoice3 和 IndexTTS-2.5 三款本地模型，分别突出综合克隆、中文方言和情绪演绎。
- 支持本人或已授权录音的真实声音克隆，一个声音可保存并切换多段参考录音。
- 提供单段配音、长稿配音和多人对话，支持 30 字试听、发音词典、表达要求、情绪与语速控制。
- 长稿自动安全分段、逐句缓存、失败重试、任务队列和重启续做；VoxCPM2 会固定前段语速与音高基线，异常片段换新结果重做，仍异常时停止合并。
- VoxCPM2 可控克隆会从较长参考录音中临时选择稳定人声片段，不修改原文件；失败重试保留已完成句子，“重新生成”会真正创建新版本而不是复用旧音频。

### 脚本与 API

- 可选配置 OpenAI 兼容文本 API，用于给定稿标记停顿、情绪和表达，正文不会被改写。
- 多人对话可从分镜稿、对白稿或混杂脚本中去掉场景、镜头和动作说明，提取角色与真正说出口的台词。
- 所有 API 功能统一使用闪光图标；未配置时按钮置灰并说明原因，处理结果确认后才应用，原稿始终保留。

### 本地模型与文件

- 模型支持一键下载、磁盘预检、速度和剩余时间、暂停续传、换源、离线导入以及整体迁移。
- 自动检测 NVIDIA CUDA；显卡条件不合适时可使用 CPU，无需预装 Python、FFmpeg 或 CUDA Toolkit。
- 项目保存稿件、声音、模型、参数和分句状态；记录可按日期试听、收藏、重命名、导出或删除。
- 导出文件名规则可自定义并记住上次目录；设置内提供检查修复、检查更新和脱敏诊断包。

### 界面、安全与隐私

- 使用 HarmonyOS Sans SC 和蓝色/薄荷液态玻璃视觉系统，三个创作主流程在 1280×720 下无需整页滚动。
- 单段配音、长稿配音和多人对话使用统一入口关系；模型卡、项目记录和设置页经过紧凑排版，能力不支持时统一锁定并说明原因。
- Electron Renderer 保持沙箱、上下文隔离与禁用 Node 集成；本地 Worker 只监听 `127.0.0.1` 并使用短期令牌。
- 不提供账号、会员、订阅、分析或遥测；“支持作者”只展示内置收款码，不接入支付 SDK 或读取交易信息。录音、声音档案和生成音频留在本机。
- 提供唯一的 `启动.cmd` 入口，并在启动前检查关键文件、用中文说明无法启动的原因。
- 启动前会检查关键文件是否完整；解压被中断时直接提示重新“全部解压”，不再交给 Windows 显示含糊的程序错误。

### 发布限制

- Windows 程序尚未进行商业代码签名，部分系统可能显示安全提示。
- IndexTTS-2.5 当前安装组合包含仅限非商业使用的辅助权重，商业用途需另行取得授权或改用另外两款模型。
- 单台 RTX 4070 的验证不能替代 Windows 10/11、无 NVIDIA、低显存和不同网络环境的完整真机矩阵。
