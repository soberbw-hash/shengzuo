# 声作使用与故障排查

## 软件打不开

1. 右键 ZIP 选择“全部解压”，不要在压缩包预览中直接运行。
2. 确认 `启动.cmd` 与 `app` 文件夹仍在同一个总文件夹。
3. 把整个文件夹复制到本机可写的短路径，例如 `D:\ShengZuo`，再试一次。
4. 不要从邮件附件预览、网盘预览、只读目录或过长路径直接运行。

部分系统安全策略会拦截未签名 EXE。当前便携包使用唯一的 `启动.cmd` 和包内运行时。

## 模型下载失败

- 至少预留模型卡片所示空间，并为下载临时文件多留一些余量。
- 检查网络、代理和防火墙是否允许访问模型插件列出的官方 HTTPS 来源。
- 回到“本地引擎”点击重试；已完成的文件会尽量复用。
- 如果关键文件校验失败，不要手动跳过校验，重新下载更安全。

## 生成失败或很慢

- 录音应为 3–60 秒、单人、清晰、无音乐和明显混响。
- 录音原文需要与实际说话内容逐字对应。
- 关闭游戏、视频剪辑和其他占用显卡的程序后重试。
- 关闭占用显卡的程序后重试；IndexTTS-2.5 建议 12GB 显存，其余两款建议 8GB 以上，声作默认一次只加载一个大模型。
- 人名、数字、缩写和外语可换成更接近读音的写法，再生成一次。

## 清理模型空间

1. 完全退出声作。
2. 在“本地引擎”或“设置”中打开模型文件夹。
3. 删除不需要模型对应的整个文件夹。
4. 重新打开声作；软件会自动刷新状态，需要时可以再次下载。

不要在模型正在下载、安装或生成时删除文件。

## 反馈问题时准备什么

请记录以下非隐私信息：

- 声作版本；
- Windows 10 或 11，以及是否存在受限策略；
- 使用的模型名称；
- 问题发生在下载、克隆、生成、试听还是导出；
- 操作后界面显示的简短错误信息。

不要直接发送私人录音、完整文稿、访问令牌或包含个人路径的整份日志。项目当前没有公开支持邮箱，请通过既有维护渠道转交上述信息。
