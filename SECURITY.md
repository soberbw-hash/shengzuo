# 安全说明

## 报告安全问题

请不要在公开 Issue 中提交 API Key、录音、完整文稿、私人路径或可直接利用的漏洞细节。

发现安全问题时，请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。报告中请包含受影响版本、Windows 版本、复现步骤、实际影响和必要的脱敏截图。维护者确认后会在修复可用时公开说明。

普通功能错误、界面问题和不含敏感信息的崩溃可以提交公开 Issue。提交诊断包前仍应自行确认其中没有不希望公开的信息。

## 支持范围

当前只维护 GitHub Releases 中最新的正式版本。Windows 程序尚未进行商业代码签名，系统安全提示本身不代表发现了漏洞；请只从本仓库的正式 Release 下载，并核对同名 SHA-256 文件。

## 安全边界

- Renderer 保持沙箱和上下文隔离，禁用 Node 集成。
- 本地语音 Worker 只监听 `127.0.0.1`，并使用短期令牌。
- API Key 使用 Windows 安全存储加密，界面不回显明文。
- 诊断信息不应包含完整文稿、原始录音、生成音频、访问令牌或不必要的私人路径。
- 模型安装只接受清单允许的固定版本，关键文件校验通过后才启用。

安全设计和威胁边界详见 [docs/security-and-privacy.md](docs/security-and-privacy.md)。
